# Plain HTTP on port 80 inside the stack. HTTPS and the domain are handled by
# the Caddy in front of it (on the public-caddy network).
:80 {
	encode zstd gzip

	handle /api/* {
		reverse_proxy soppkart-backend:8000
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
